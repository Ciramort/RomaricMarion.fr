{% extends 'partials/base.html.twig' %}
{% block content %}
<section id="presentation">
    <div class="row">
        <div class="nine columns main-col">
            <h2>{{ form.message }}</h2>
            {{ content }}

            {% include "forms/data.html.twig" %} <br />
            Retourner à l'<a href="{{ base_url_absolute }}">accueil</a>.
        </div>
    </div>
</section>
{% endblock %}
